<h1>Blog Posts</h1>

<% @posts.each do |post| %>
  <div class="bg-light mt-3 border rounded card-body">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>

    <%= link_to "Read More", post_path(post), {class: 'btn btn-light border rounded'} %>
  </div>
<% end %>